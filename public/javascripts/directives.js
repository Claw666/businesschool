angular.module('angularApp').directive('svgMapLevelThree', ['$compile', function ($compile) {
    return {
        restrict: 'A',
        templateUrl: '/uploads/maps/level-3.svg',
        link: function (scope, element, attrs) {
            
        }
    }
}]);

angular.module('angularApp').directive('svgMapLevelFour', ['$compile', function ($compile) {
    return {
        restrict: 'A',
        templateUrl: '/uploads/maps/level-4.svg',
        link: function (scope, element, attrs) {
            
        }
    }
}]);

angular.module('angularApp').directive('svgMapLevelFive', ['$compile', function ($compile) {
    return {
        restrict: 'A',
        templateUrl: '/uploads/maps/level-5.svg',
        link: function (scope, element, attrs) {
            
        }
    }
}]);

angular.module('angularApp').directive('svgMapLevelSix', ['$compile', function ($compile) {
    return {
        restrict: 'A',
        templateUrl: '/uploads/maps/level-6.svg',
        link: function (scope, element, attrs) {
            
        }
    }
}]);